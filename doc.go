// Package rsts is a RSTS/E V7.2 lookalike: BASIC-PLUS, a Bye/Ready CLI,
// PPN disk storage, and jobs on a console, Telnet, or serial line.
//
// It is a timesharing user environment, not a PDP-11 CPU emulator and not
// BASIC-PLUS-2. Programs compile to a private bytecode (not Digital .BAC
// P-code). The portrayed system stays V7.2; the dash level in Version is
// this project's own release number.
package rsts
